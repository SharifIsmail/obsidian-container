# Web UI credentials (for browser access to Obsidian desktop)
CUSTOM_USER=your-username
PASSWORD=your-password

# Container user/group IDs (match your host user)
PUID=1000
PGID=1000

TZ=Etc/UTC
RESTART_APP=true

# Path to rclone config directory (containing rclone.conf)
# See README for setup instructions
#RCLONE_CONFIG=/path/to/rclone-config
